/target
/data
node_modules/
.DS_Store

# SvelteKit / Vite
frontend/.svelte-kit/
frontend/build/
frontend/dist/

# the server embeds this at compile time; it is a build artifact. The crate
# compiles without it, so there is nothing to keep the directory alive for.
crates/server/assets/

# coverage
lcov.info
*.profraw
frontend/coverage/

# the layout suite seeds its own markdown tree and never touches ./data
/.e2e-data
frontend/test-results/
frontend/playwright-report/

# Local agent permission grants, not a project setting.
.claude/
