// A pure single-page app: routing is client-side and every screen needs the API,
// so there is nothing meaningful to render or prerender on the server.
export const ssr = false;
export const prerender = false;
